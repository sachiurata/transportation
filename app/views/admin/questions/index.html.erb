<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
  <h1 class="h2">質問管理: <%= @survey.survey_name %></h1>
  <div class="btn-toolbar mb-2 mb-md-0">
    <%= link_to "新規質問作成", new_admin_survey_question_path(@survey), class: "btn btn-sm btn-primary" %>
  </div>
</div>

<h3 class="h4">登録済み質問一覧</h3>
<% if @questions.any? %>
  <ul class="list-group">
    <% @questions.each do |question| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= question.text %>
        <div>
          <%= link_to "編集", edit_admin_survey_question_path(@survey, question), class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "削除", admin_survey_question_path(@survey, question), data: { turbo_method: :delete, turbo_confirm: "この質問と関連する回答も全て削除されます。本当に削除しますか？" }, class: "btn btn-sm btn-outline-danger ms-2" %>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>まだ質問は登録されていません。</p>
<% end %>

<div class="mt-4">
  <%= link_to "アンケート一覧に戻る", admin_surveys_path, class: "btn btn-secondary" %>
</div>
